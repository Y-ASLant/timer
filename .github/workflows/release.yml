name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  release:
    strategy:
      fail-fast: false
      matrix:
        platform:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            name: Windows-x64
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            name: Linux-x64
            # 未来可以添加 macOS 平台
            # - os: macos-latest
            #   target: x86_64-apple-darwin
            #   name: macOS-x64
            # - os: macos-latest
            #   target: aarch64-apple-darwin
            #   name: macOS-arm64

    runs-on: ${{ matrix.platform.os }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform.target }}

      - name: Install Linux dependencies
        if: matrix.platform.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev \
            build-essential \
            curl \
            wget \
            file \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            imagemagick

      - name: Generate PNG icons from ICO (Linux)
        if: matrix.platform.os == 'ubuntu-22.04'
        run: |
          convert icons/icon.ico[0] -resize 32x32 icons/32x32.png
          convert icons/icon.ico[0] -resize 128x128 icons/128x128.png
          convert icons/icon.ico[0] -resize 256x256 icons/128x128@2x.png

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install Tauri CLI
        run: cargo install tauri-cli --version "^2.0.0" --locked

      - name: Build Tauri app
        working-directory: ./src-tauri
        run: cargo tauri build --verbose

      - name: Compress binaries (Linux)
        if: matrix.platform.os == 'ubuntu-22.04'
        run: |
          # 使用UPX进一步压缩二进制文件
          sudo apt-get install -y upx
          find src-tauri/target/release -name "countdown-timer" -type f -exec upx --best --lzma {} \; || true

      - name: Get version from tag
        id: get_version
        shell: bash
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Prepare release files (Windows)
        if: matrix.platform.os == 'windows-latest'
        shell: pwsh
        run: |
          New-Item -Path "release" -ItemType Directory -Force
          # 复制并重命名文件
          Get-ChildItem -Path "src-tauri/target/release/bundle/msi/*.msi" -ErrorAction SilentlyContinue | ForEach-Object {
            $newName = $_.Name -replace 'countdown-timer', '计时器'
            Copy-Item $_.FullName -Destination "release/$newName"
          }
          Get-ChildItem -Path "src-tauri/target/release/bundle/nsis/*.exe" -ErrorAction SilentlyContinue | ForEach-Object {
            $newName = $_.Name -replace 'countdown-timer', '计时器'
            Copy-Item $_.FullName -Destination "release/$newName"
          }
          Get-ChildItem -Path "release/" -Recurse | ForEach-Object { Write-Host $_.Name }

      - name: Prepare release files (Linux)
        if: matrix.platform.os == 'ubuntu-22.04'
        run: |
          mkdir -p release
          # 复制并重命名 DEB 文件
          find src-tauri/target/release/bundle/deb -name "*.deb" 2>/dev/null | while read file; do
            newname=$(basename "$file" | sed 's/countdown-timer/计时器/g')
            cp "$file" "release/$newname"
          done
          # 复制并重命名 RPM 文件
          find src-tauri/target/release/bundle/rpm -name "*.rpm" 2>/dev/null | while read file; do
            newname=$(basename "$file" | sed 's/countdown-timer/计时器/g')
            cp "$file" "release/$newname"
          done
          ls -lh release/

      - name: Upload release assets
        uses: softprops/action-gh-release@v2
        with:
          files: release/*
          draft: false
          prerelease: false
          generate_release_notes: true
          fail_on_unmatched_files: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
